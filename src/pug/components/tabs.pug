mixin Tabs(props)
    -
        const { values = [], attr } = props || {};

    .flex.gap-2.flex-row.bg-neutral-800.p-1.rounded-md.w-fit.items-center.justify-center.h-8.m-0
        input(type="hidden" name=`attr_${attr}` value=values[0])
        each tab in values
            +Button({name: tab, i18n: tab, variant: "tab", type: "action"})
