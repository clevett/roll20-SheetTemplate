//- Helpful functions used to handle Roll20's attributes naming conventions
- const dashedName = name => name.replace(/ /g, '-');
- const attrName = name => name.replace(/ /g, '_');
- const removeSpace = name => name.replace(/ /g, '');
- const toLowerCase = name => name.toLowerCase();

//- Used for character stats and reusable variables. Often overlaps constants.ts
//- - const mentalAttributes = ["awareness", "intellect", "charisma"];
//- - const physicalAttributes = ["strength", "dexterity", "endurance"];
//- - const attributes = [...mentalAttributes,  ...physicalAttributes];